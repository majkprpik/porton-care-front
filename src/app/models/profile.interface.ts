export interface Profile {
  id: string;
  first_name: string;
  last_name: string;
  role: string;
  email?: string;
  // is_cleaning_staff?: boolean;
  // is_available?: boolean;
  created_at?: string;
  phone_number: string;
} 